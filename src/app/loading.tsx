
import { LoadingSpinner } from '@/components/LoadingSpinner';

export default function RootLoading() {
  // This UI will be shown as a fallback while the main page content is loading
  return <LoadingSpinner />;
}
