
import { LoadingSpinner } from '@/components/LoadingSpinner';

export default function FavoritesLoading() {
  // This UI will be shown as a fallback while the favorites page content is loading
  return <LoadingSpinner />;
}
